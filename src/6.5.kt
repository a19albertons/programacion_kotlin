fun main() {
    var entrada = readln().toInt()
    while (entrada!=0 || entrada>1000) {
        for (item in 1..entrada) {
            print(1)
        }
        println()

//        Procesa la reentrada
        entrada= readln().toInt()
    }
}